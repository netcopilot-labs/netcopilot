# Fixtures

Synthetic data for tests and the local demo. **No real network data ever lives here.**

A minimal synthetic seed graph (devices, links, findings) lands in F1-3 so the MCP
spine and tools are testable end-to-end. The fuller synthetic demo topology is built
later (F4). Users bring their own network; these fixtures are only a worked example.
